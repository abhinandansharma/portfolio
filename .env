# Public URL of the deployed site, with trailing slash. Used for canonical/og/JSON-LD in
# public/index.html and for sitemap.xml + robots.txt (scripts/seo.js). Change here when the
# site moves to a custom domain, and update "homepage" in package.json to match.
REACT_APP_SITE_URL=https://abhinandansharma.github.io/portfolio/
